typedef struct PATH
{
    // Bloqueado
    int blocked;
    // COORDENADAS
    float x, y;
    int width, height;
    // IMAGE
    ALLEGRO_BITMAP * image;
    // CONTEXT
    // GUI_CONTEXT *ctx;
    
} PATH;
